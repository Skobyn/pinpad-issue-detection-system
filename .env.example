# Database connection - local file or MotherDuck URI
PINPAD_DB=md:pinpad_analyzer
# PINPAD_DB=./data/pinpad_analyzer.duckdb

# MotherDuck token (required for md: URIs)
MOTHERDUCK_TOKEN=

# GCS credentials (optional - uses ADC by default)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# POS upload agent config
# GCS_BUCKET=pinpad-logs
# UPLOAD_WATCH_DIR=/path/to/pos/logs

# Cloud Run (set automatically in Cloud Run environment)
# PORT=8080
# LOG_LEVEL=INFO
